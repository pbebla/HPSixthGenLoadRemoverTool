#pragma once
#include <windows.h>
#include <CommCtrl.h>
class WindowLayout
{
	private:
		HWND hwndButton, hwndButton2, hwndButton3, hwndButton4, hwndPicBox, hwndProcessList;
	public:
		WindowLayout(HWND);

};

